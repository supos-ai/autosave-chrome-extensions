{
  "name": "proxy",
  "version": "1.0.0",
  "description": "rewrite fetch for proxy",
  "private": true,
  "keywords": [
    "proxy",
    "fetch"
  ],
  "author": "sunzhiqi <sunzhiqi@live.com>",
  "homepage": "https://github.com/supos-ai/autosave-chrome-extensions#readme",
  "license": "ISC",
  "directories": {
    "src": "src"
  },
  "files": [
    "src"
  ],
  "main": "./build/index.js",
  "types": "./build/types/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/supos-ai/autosave-chrome-extensions.git"
  },
  "scripts": {
    "test": "jest",
    "dev": "rimraf build && rollup -c --watch && yarn build:type",
    "build": "rimraf build && rollup -c && yarn build:type",
    "build:type": "tsc -b ."
  },
  "bugs": {
    "url": "https://github.com/supos-ai/autosave-chrome-extensions/issues"
  },
  "dependencies": {
    "indexdb": "*"
  }
}
